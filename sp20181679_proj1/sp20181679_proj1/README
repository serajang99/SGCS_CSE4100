#환경 구성
- cspro 서버인 리눅스 환경에서 코딩하였다.
- 기본적인 GCC컴파일러의 ANSI C 표준을 준수한다.

#컴파일 방법
- Makefile 을 이용해 컴파일한다.
  해당 프로젝트 폴더에서 "make"를 이용해 컴파일하는데, 여기에 꼭 opcode.txt가 있어야 한다.
  소스코드만 남기고 싶으면 "make clean"을 통해 가능하다.

#실행 방법
- Makefile 을 이용해서 가능하다.
  실행 파일은 "20181679.out"으로 고정되어있다.
  "./20181679.out"로 실행시킨다.
  실행시키면 unix shell과 유사한 입력 프롬포트 상태가 된다.
